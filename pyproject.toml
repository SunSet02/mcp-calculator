[project]
name = "mcp-calculator"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["mcp"]

[project.scripts]
mcp_calculator = "mcp_calculator.calculator:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"